// next.config.ts
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    appDir: true, // Only needed if you're using the /app directory (Next.js 13+)
  },
};

export default nextConfig;
